

<!--
FILE NAME: index.html
COMPONENT OVERVIEW:
This file is the core structural entry point for the Movie Review System web application.
It integrates a component-based design architecture using HTML5 semantics in combination
with TailwindCSS utility classes to form the foundational layout.

FEATURES AND WORKING:
1. Meta Configuration: Bootstraps the viewport for responsive scaling across mobile devices.
2. Styling Architecture: Links external dependencies like FontAwesome for iconography and Google Fonts.
3. Custom Tailwind Configuration: Overrides default token scales to implement the 'cyber' theme.
4. Navigation Controls: Defines an interactive header that switches visibility layers across tabs.
5. Search Interfaces: Builds the interactive form elements required to parse movie queries.
6. Detail Overlays: Contains the modal pop-ups that draw movie data without page reloads.
-->

<!DOCTYPE html>

<html lang="en">

<head>

    <meta charset="UTF-8">

    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <title>Movie Nova | Full Stack</title>

    <script src="https://cdn.tailwindcss.com"></script>
    <!-- [EXTERNAL INTEGRATION]: Pulling core processing logic from disconnected dependency scripts -->

    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">

    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=Outfit:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">

    <link rel="stylesheet" href="css/style.css">

    <script>
    <!-- [EXTERNAL INTEGRATION]: Pulling core processing logic from disconnected dependency scripts -->

        tailwind.config = {

            theme: {

                extend: {

                    fontFamily: { 'outfit': ['Outfit', 'sans-serif'], 'inter': ['Inter', 'sans-serif'] },

                    colors: { cyber: { black: '#0a0a0a', panel: '#171717', cyan: '#3b82f6', purple: '#e50914' } }

                }

            }

        }

    </script>

<body class="h-screen flex flex-col overflow-hidden text-lg relative bg-cyber-black text-gray-200 font-inter">



    <div class="stars-container"><div class="nebula"></div><div class="nebula-2"></div><div class="stars-small"></div><div class="stars-medium"></div></div>



    <nav id="mainNavbar" class="glass-panel border-b-0 h-20 flex items-center justify-between px-4 md:px-8 shrink-0 z-30 mx-2 md:mx-4 mt-4 rounded-2xl relative transition-all duration-300">

        <div class="flex items-center gap-4 group cursor-pointer" onclick="switchMainTab('reviews'); resetApp()">

            <div class="relative w-10 h-10 flex items-center justify-center">

                <i class="fa-solid fa-meteor text-3xl text-cyber-cyan animate-pulse"></i>

            </div>

            <div class="hidden md:block">

                <h1 class="font-outfit text-xl font-bold tracking-wider text-white">MOVIE <span class="text-cyber-cyan">NOVA</span></h1>

            </div>

        </div>

        <div class="flex items-center gap-6 font-outfit text-xs md:text-sm tracking-widest">

            <button id="nav-reviews" onclick="switchMainTab('reviews')" class="tab-button active-tab text-gray-400 border-b-2 border-transparent pb-1 hover:text-white transition-all"><i class="fa-solid fa-film mr-2"></i>REVIEWS</button>
    <!-- [USER INTERACTION NODE]: Instantiating physical listener control element mapping click functions -->

            <button id="nav-blog" onclick="switchMainTab('blog')" class="tab-button text-gray-400 border-b-2 border-transparent pb-1 hover:text-white transition-all"><i class="fa-solid fa-satellite mr-2"></i>BLOG</button>
    <!-- [USER INTERACTION NODE]: Instantiating physical listener control element mapping click functions -->

            <button id="nav-profile" onclick="switchMainTab('profile')" class="tab-button text-gray-400 border-b-2 border-transparent pb-1 hover:text-white transition-all"><i class="fa-solid fa-user-astronaut mr-2"></i>PROFILE</button>
    <!-- [USER INTERACTION NODE]: Instantiating physical listener control element mapping click functions -->

        </div>

        <div class="flex items-center gap-4">

            <button onclick="switchMainTab('admin')" class="text-gray-500 hover:text-red-500 transition-colors" title="Admin Panel"><i class="fa-solid fa-shield-halved"></i></button>
    <!-- [USER INTERACTION NODE]: Instantiating physical listener control element mapping click functions -->

            <div class="w-10 h-10 rounded-full bg-gradient-to-br from-cyber-cyan to-cyber-purple p-[2px]">

                <img id="navbarAvatar" src="" class="w-full h-full rounded-full opacity-90 object-cover bg-black">

            </div>

        </div>

    </nav>



    <main class="flex-1 relative overflow-hidden flex flex-col z-10 p-2 md:p-4">

        

        <div id="tab-reviews" class="tab-content active flex-1 min-h-0 w-full max-w-7xl mx-auto flex flex-col">

             <div class="flex-shrink-0 py-6 px-4 text-center relative mb-4">

                <h2 class="font-outfit text-3xl md:text-5xl font-black text-white mb-6 uppercase">Explore <span class="text-transparent bg-clip-text bg-gradient-to-r from-cyber-cyan to-cyber-purple">MOVIE-VERSE</span></h2>

                <div class="max-w-3xl mx-auto relative group z-20">

                    <div class="relative flex items-center bg-cyber-panel border border-white/10 rounded-full p-2 shadow-2xl backdrop-blur-md">

                        <i class="fa-solid fa-magnifying-glass text-gray-500 ml-4 text-lg"></i>

                        <input type="text" id="searchInput" class="w-full bg-transparent border-none text-white px-4 py-3 focus:ring-0 font-inter text-xl placeholder-gray-500 outline-none h-12" placeholder="Search titles..." onkeypress="handleEnter(event)">
    <!-- [DATA CAPTURE MECHANISM]: Constructing binary textual input buffer waiting for keypress combinations -->

                        <button onclick="initSearch()" class="bg-white/5 hover:bg-white/10 text-cyber-cyan px-6 py-2 rounded-full font-outfit font-bold text-sm border border-white/5">Launch</button>
    <!-- [USER INTERACTION NODE]: Instantiating physical listener control element mapping click functions -->

                    </div>

                </div>

            </div>

            <div id="resultsContainer" class="flex-1 overflow-y-auto px-4 pb-4 grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 xl:grid-cols-5 gap-6 content-start custom-scrollbar"></div>

            

            <div id="paginationControls" class="hidden flex justify-center items-center gap-4 py-8 shrink-0">

                <button id="prevBtn" onclick="changePage(-1)" class="bg-white/5 hover:bg-white/10 text-white px-4 py-2 rounded-lg font-outfit text-sm border border-white/10 disabled:opacity-50 disabled:cursor-not-allowed"><i class="fa-solid fa-chevron-left"></i> PREV</button>
    <!-- [USER INTERACTION NODE]: Instantiating physical listener control element mapping click functions -->

                <span id="pageIndicator" class="font-mono text-cyber-cyan text-lg">01</span>

                <button id="nextBtn" onclick="changePage(1)" class="bg-white/5 hover:bg-white/10 text-white px-4 py-2 rounded-lg font-outfit text-sm border border-white/10"><i class="fa-solid fa-chevron-right"></i> NEXT</button>
    <!-- [USER INTERACTION NODE]: Instantiating physical listener control element mapping click functions -->

            </div>

        </div>



        <div id="tab-blog" class="tab-content flex-1 min-h-0 w-full max-w-4xl mx-auto overflow-hidden">

             <div class="flex-1 overflow-y-auto p-4 custom-scrollbar">

                <div class="text-center mb-8 pt-4"><h2 class="font-outfit text-3xl font-black text-white uppercase">Stellar Logs</h2></div>

                <div class="glass-panel p-6 rounded-xl border border-white/10 mb-8 relative overflow-hidden">
    <!-- [UI COMPONENT OVERVIEW]: Structural Glassmorphism container encapsulating interface features -->

                    <h3 class="font-outfit text-lg text-white mb-4"><i class="fa-solid fa-signal text-cyber-cyan text-xs"></i> NEW TRANSMISSION</h3>

                    <div class="flex flex-col gap-3">

                        <input type="text" id="blogTitleInput" placeholder="Subject / Title" class="bg-black/30 border border-white/10 rounded-lg p-3 text-white font-inter outline-none">
    <!-- [DATA CAPTURE MECHANISM]: Constructing binary textual input buffer waiting for keypress combinations -->

                        <textarea id="blogContentInput" rows="3" placeholder="Enter log data..." class="bg-black/30 border border-white/10 rounded-lg p-3 text-white font-inter outline-none resize-none"></textarea>

                        <div class="flex flex-col md:flex-row gap-3 items-center">

                             <div class="flex-1 w-full"><input type="file" id="blogMediaInput" accept="image/*,video/*" class="w-full bg-black/30 border border-white/10 rounded-lg p-2 text-sm text-cyber-cyan font-mono"></div>
    <!-- [DATA CAPTURE MECHANISM]: Constructing binary textual input buffer waiting for keypress combinations -->

                            <button onclick="submitBlogPost()" class="bg-cyber-purple hover:bg-purple-600 text-white px-6 py-2 rounded-lg font-outfit text-sm">UPLOAD</button>
    <!-- [USER INTERACTION NODE]: Instantiating physical listener control element mapping click functions -->

                        </div>

                    </div>

                </div>

                <div id="blogFeed" class="space-y-6 pb-20"></div>

            </div>

        </div>



        <div id="tab-profile" class="tab-content flex-1 min-h-0 w-full max-w-2xl mx-auto overflow-y-auto custom-scrollbar p-4 pb-24">

            <div class="glass-panel p-8 rounded-2xl border border-white/10 mt-10 relative overflow-hidden flex-shrink-0">
    <!-- [UI COMPONENT OVERVIEW]: Structural Glassmorphism container encapsulating interface features -->

                <div id="otpSection" class="text-center">

                    <div class="w-20 h-20 bg-cyber-cyan/10 rounded-full flex items-center justify-center mx-auto mb-6 border border-cyber-cyan/30">

                        <i class="fa-solid fa-mobile-screen text-3xl text-cyber-cyan"></i>

                    </div>

                    <h2 class="font-outfit text-2xl text-white mb-2">SECURITY CHECK</h2>

                    <p class="font-inter text-gray-400 mb-6">Verify your communicator link.</p>

                    <div id="phoneStep">

                        <input type="number" id="phoneNumber" placeholder="Enter Phone Number" class="w-full bg-black/30 border border-white/20 rounded-lg p-4 text-white font-inter text-lg outline-none mb-4 text-center">
    <!-- [DATA CAPTURE MECHANISM]: Constructing binary textual input buffer waiting for keypress combinations -->

                        <button onclick="sendOTP()" class="w-full bg-cyber-cyan text-black font-bold font-outfit py-3 rounded-lg hover:bg-cyan-300 transition-all">SEND ACCESS CODE</button>
    <!-- [USER INTERACTION NODE]: Instantiating physical listener control element mapping click functions -->

                    </div>

                    <div id="otpVerifyStep" class="hidden">

                        <input type="number" id="otpCode" placeholder="Enter Code" class="w-full bg-black/30 border border-white/20 rounded-lg p-4 text-white font-inter text-lg outline-none mb-4 text-center tracking-[1em]">
    <!-- [DATA CAPTURE MECHANISM]: Constructing binary textual input buffer waiting for keypress combinations -->

                        <button onclick="verifyOTP()" class="w-full bg-green-500 text-black font-bold font-outfit py-3 rounded-lg hover:bg-green-400 transition-all">VERIFY</button>
    <!-- [USER INTERACTION NODE]: Instantiating physical listener control element mapping click functions -->

                        <p class="text-xs text-gray-500 mt-4 cursor-pointer hover:text-white" onclick="resetOTP()">Resend Code</p>

                    </div>

                </div>



                <div id="profileFormSection" class="hidden">

                    <h2 class="font-outfit text-2xl text-white mb-6 border-b border-white/10 pb-4">COMMANDER PROFILE</h2>

                    <div class="flex flex-col items-center mb-8">

                        <img id="profilePreview" src="" class="w-32 h-32 rounded-full border-2 border-white/20 object-cover mb-4">

                        <p class="text-gray-500 font-mono text-xs">CURRENT AVATAR</p>

                    </div>

                    <div class="space-y-6">

                        <div><label class="block text-cyber-cyan font-outfit text-xs tracking-widest mb-2">CALLSIGN</label><input type="text" id="newUsername" class="w-full bg-black/30 border border-white/20 rounded-lg p-4 text-white font-inter text-lg outline-none"></div>
    <!-- [DATA CAPTURE MECHANISM]: Constructing binary textual input buffer waiting for keypress combinations -->

                        <div><label class="block text-cyber-purple font-outfit text-xs tracking-widest mb-2">NEW AVATAR</label><input type="file" id="newAvatar" accept="image/*" class="w-full bg-black/30 border border-white/20 rounded-lg p-3 text-gray-300 font-mono text-sm"></div>
    <!-- [DATA CAPTURE MECHANISM]: Constructing binary textual input buffer waiting for keypress combinations -->

                        <button onclick="saveUserProfile()" class="w-full bg-gradient-to-r from-cyber-cyan to-cyber-purple text-white font-bold font-outfit py-4 rounded-lg tracking-widest hover:opacity-90">UPDATE IDENTITY</button>
    <!-- [USER INTERACTION NODE]: Instantiating physical listener control element mapping click functions -->

                        <p class="text-green-400 font-mono text-xs text-center hidden" id="profileSavedMsg">IDENTITY SAVED SUCCESSFULLY</p>

                    </div>

                </div>

            </div>

        </div>



        <div id="tab-admin" class="tab-content flex-1 min-h-0 w-full max-w-6xl mx-auto overflow-y-auto custom-scrollbar p-4 relative">

            

            <div id="adminLoginOverlay" class="absolute inset-0 z-50 flex items-center justify-center bg-black/80 backdrop-blur-md">

                <div class="glass-panel p-8 rounded-xl border border-red-500/50 w-full max-w-md text-center relative">
    <!-- [UI COMPONENT OVERVIEW]: Structural Glassmorphism container encapsulating interface features -->

                    <button onclick="switchMainTab('reviews')" class="absolute top-4 right-4 text-gray-500 hover:text-white transition-colors" title="Close Admin"><i class="fa-solid fa-xmark text-xl"></i></button>
    <!-- [USER INTERACTION NODE]: Instantiating physical listener control element mapping click functions -->

                    <i class="fa-solid fa-user-secret text-4xl text-red-500 mb-4"></i>

                    <h2 class="font-outfit text-xl text-white mb-4">CLASSIFIED ACCESS</h2>

                    <input type="password" id="adminPasswordInput" placeholder="Enter Access Key" class="w-full bg-black/50 border border-red-500/30 rounded p-3 text-white text-center font-mono mb-4 outline-none focus:border-red-500">
    <!-- [DATA CAPTURE MECHANISM]: Constructing binary textual input buffer waiting for keypress combinations -->

                    <button onclick="adminLogin()" class="w-full bg-red-600 hover:bg-red-700 text-white font-outfit py-3 rounded transition-colors">AUTHENTICATE</button>
    <!-- [USER INTERACTION NODE]: Instantiating physical listener control element mapping click functions -->

                </div>

            </div>



            <div id="adminPanelContent" class="hidden glass-panel p-6 rounded-xl border border-red-500/30">

                <div class="flex flex-col md:flex-row items-center justify-between mb-8 border-b border-red-500/20 pb-4 gap-4">

                    <h2 class="font-outfit text-2xl text-red-500 font-bold"><i class="fa-solid fa-lock mr-2"></i> COMMAND CENTER</h2>

                    <div class="flex gap-4 items-center">

                        <div class="flex gap-2">

                            <select id="statsFilter" onchange="handleFilterChange()" class="bg-black/40 border border-white/10 text-white text-xs rounded px-3 py-2 outline-none font-outfit cursor-pointer">

                                <option value="today">TODAY</option>

                                <option value="weekly">WEEKLY</option>

                                <option value="monthly">MONTHLY</option>

                                <option value="yearly">YEARLY</option>

                                <option value="date">SPECIFIC DATE</option>

                            </select>

                            <input type="date" id="statsDateInput" onchange="refreshAdminData()" class="hidden bg-black/40 border border-white/10 text-white text-xs rounded px-3 py-2 outline-none font-mono">
    <!-- [DATA CAPTURE MECHANISM]: Constructing binary textual input buffer waiting for keypress combinations -->

                            <button onclick="refreshAdminData()" class="text-cyber-cyan hover:text-white px-3"><i class="fa-solid fa-rotate"></i></button>
    <!-- [USER INTERACTION NODE]: Instantiating physical listener control element mapping click functions -->

                        </div>

                        <button onclick="switchMainTab('reviews')" class="bg-red-500/10 hover:bg-red-500/20 text-red-500 border border-red-500/30 rounded px-4 py-2 font-outfit text-xs transition-colors flex items-center gap-2"><i class="fa-solid fa-door-open"></i> EXIT</button>
    <!-- [USER INTERACTION NODE]: Instantiating physical listener control element mapping click functions -->

                    </div>

                </div>



                <div class="grid grid-cols-1 md:grid-cols-2 gap-4 mb-8">

                    <div class="bg-black/40 p-4 rounded-lg border border-white/5 flex justify-between items-center">

                        <div><h4 class="text-gray-500 text-xs font-outfit">NEW REVIEWS</h4></div>

                        <p class="text-3xl text-cyber-cyan font-bold" id="adminReviewCount">0</p>

                    </div>

                    <div class="bg-black/40 p-4 rounded-lg border border-white/5 flex justify-between items-center">

                        <div><h4 class="text-gray-500 text-xs font-outfit">NEW BLOGS</h4></div>

                        <p class="text-3xl text-purple-500 font-bold" id="adminBlogCount">0</p>

                    </div>

                </div>



                <h3 class="font-outfit text-white mb-4">DATABASE ENTRIES</h3>

                <div class="overflow-x-auto max-h-[500px] custom-scrollbar">

                    <table class="w-full text-left border-collapse">

                        <thead class="sticky top-0 bg-black z-10"><tr class="text-xs text-gray-500 font-outfit border-b border-white/10"><th class="p-3">ID</th><th class="p-3">TYPE</th><th class="p-3">USER</th><th class="p-3">CONTENT</th><th class="p-3">ACTION</th></tr></thead>

                        <tbody id="adminTableBody" class="text-sm font-inter text-gray-300"></tbody>

                    </table>

                </div>

            </div>

        </div>



        <div id="detailView" class="fixed inset-0 z-50 bg-black/95 backdrop-blur-xl flex items-center justify-center p-0 md:p-8 opacity-0 pointer-events-none transition-all duration-300 scale-95" onclick="closeDetailView(event)">

            <div class="w-full max-w-6xl h-full md:h-[90vh] glass-panel md:rounded-3xl flex flex-col md:flex-row relative border border-white/10 overflow-hidden" onclick="event.stopPropagation()">

                <button onclick="closeDetailViewForce()" class="absolute top-4 right-4 z-[60] w-12 h-12 bg-black/60 text-white rounded-full flex items-center justify-center border border-white/20"><i class="fa-solid fa-xmark text-xl"></i></button>
    <!-- [USER INTERACTION NODE]: Instantiating physical listener control element mapping click functions -->

                <div class="w-full md:w-1/3 relative bg-black shrink-0 h-64 md:h-auto"><img id="detailPoster" src="" class="w-full h-full object-cover opacity-60"><div class="absolute bottom-0 left-0 p-6 w-full"><h2 id="detailTitle" class="text-2xl md:text-4xl font-bold text-white font-outfit leading-none mb-2">Title</h2><div class="flex items-center gap-4 text-sm font-inter text-gray-300"><span id="detailYear" class="text-cyber-cyan">Year</span><span id="detailType" class="uppercase">Movie</span></div></div></div>

                <div class="flex-1 flex flex-col bg-cyber-dark/80 relative overflow-hidden h-full">

                    <div class="flex items-center gap-6 px-6 pt-6 border-b border-white/5 font-outfit text-sm shrink-0 bg-cyber-dark/95 z-10"><button class="pb-4 border-b-2 border-cyber-cyan text-white">REVIEWS</button><div class="ml-auto"><select id="sortSelect" onchange="renderReviews()" class="bg-black/40 border border-white/10 text-gray-300 text-xs rounded-lg px-3 py-1 outline-none"><option value="newest">NEWEST</option><option value="oldest">OLDEST</option><option value="highest">HIGH</option><option value="lowest">LOW</option></select></div></div>
    <!-- [USER INTERACTION NODE]: Instantiating physical listener control element mapping click functions -->

                    <div id="reviewsList" class="flex-1 overflow-y-auto p-6 space-y-4 custom-scrollbar"></div>

                    <div class="p-6 bg-black/40 border-t border-white/5 shrink-0"><div class="flex justify-between items-center mb-4"><div class="flex items-center gap-3"><img id="reviewUserAvatar" src="" class="w-8 h-8 rounded-full"><span id="reviewUsernameDisplay" class="text-sm text-cyber-cyan font-outfit">User</span></div><div class="star-rating flex gap-2 text-xl text-gray-600" id="starInput"><i class="fa-solid fa-star" data-val="1"></i><i class="fa-solid fa-star" data-val="2"></i><i class="fa-solid fa-star" data-val="3"></i><i class="fa-solid fa-star" data-val="4"></i><i class="fa-solid fa-star" data-val="5"></i></div></div><div class="relative"><textarea id="reviewText" rows="2" class="w-full bg-white/5 border border-white/10 rounded-xl p-4 text-sm text-gray-200 outline-none resize-none" placeholder="Transmission content..."></textarea><button onclick="submitReview()" class="absolute bottom-3 right-3 text-cyber-cyan"><i class="fa-solid fa-paper-plane"></i></button></div></div>
    <!-- [USER INTERACTION NODE]: Instantiating physical listener control element mapping click functions -->

                </div>

            </div>

        </div>

        <div id="toast" class="fixed top-24 right-4 z-[70] transform translate-x-full transition-transform duration-300"><div id="toastPanel" class="glass-panel px-6 py-4 rounded-xl border-l-4 border-cyber-cyan flex items-center gap-4 bg-black/90"><i id="toastIcon" class="fa-solid fa-check text-cyber-cyan"></i><div><h4 id="toastTitle" class="font-outfit text-sm text-white">SUCCESS</h4><p class="font-inter text-xs text-gray-400" id="toastMsg">Completed.</p></div></div></div>



    <script src="js/config.js"></script><script src="js/utils.js"></script><script src="js/state.js"></script>
    <!-- [EXTERNAL INTEGRATION]: Pulling core processing logic from disconnected dependency scripts -->

    <script src="js/reviews.js"></script><script src="js/blog.js"></script><script src="js/profile.js"></script><script src="js/admin.js"></script><script src="js/main.js"></script>
    <!-- [EXTERNAL INTEGRATION]: Pulling core processing logic from disconnected dependency scripts -->

</body>

</html>





/*
FILE NAME: style.css
COMPONENT OVERVIEW:
While Tailwind handles structural styling, this custom stylesheet implements the complex
thematic elements that provide the visual identity of the platform, including animations.

FEATURES AND WORKING:
1. Body Resets: Hardcodes the primary dark background and font hierarchies to prevent browser conflicts.
2. Starfield Animations: Creates a parallax effect using CSS box-shadow wizardry to simulate a galaxy.
3. Scrollbar UI: Replaces the default browser scrollbars with custom thin-track digital variants.
4. Glassmorphism: Utilizes backdrop-filter properties to blur elements behind UI panels, producing depth.
5. Micro-interactions: Controls the cubic-bezier fading and scale effects when hovering on movie grids.
*/

body {

    background-color: #0a0a0a;

    color: #e2e8f0;

    font-family: 'Inter', sans-serif;

    overflow-x: hidden;

}



/* --- SPACE BACKGROUND (Stars & Nebula) --- */

.stars-container {

    position: fixed;

    top: 0; left: 0; width: 100%; height: 100%;

    pointer-events: none;

    z-index: 0;

    overflow: hidden;

}



.stars-small {

    width: 1px; height: 1px; background: transparent;
  /* [VISUAL LAYER]: Dictating color matrix adjustments and image overlays upon element initialization */

    box-shadow: 1744px 122px #FFF , 134px 1321px #FFF , 92px 859px #FFF, 1843px 321px #FFF, 543px 231px #FFF, 1233px 432px #FFF, 43px 1234px #FFF, 754px 942px #FFF, 1532px 142px #FFF, 234px 1754px #FFF, 154px 543px #FFF, 1865px 865px #FFF;

    animation: twinkle 5s infinite linear;

}

.stars-medium {

    width: 2px; height: 2px; background: transparent;
  /* [VISUAL LAYER]: Dictating color matrix adjustments and image overlays upon element initialization */

    box-shadow: 632px 432px #FFF , 1643px 132px #FFF , 234px 876px #FFF, 876px 234px #FFF, 1432px 876px #FFF, 321px 1543px #FFF;

    animation: twinkle 8s infinite linear;

}



.nebula {

    position: absolute;

    width: 600px; height: 600px;

    background: radial-gradient(circle, rgba(114, 9, 183, 0.08) 0%, transparent 70%);
  /* [VISUAL LAYER]: Dictating color matrix adjustments and image overlays upon element initialization */

    top: 20%; left: 50%; transform: translate(-50%, -50%);

    filter: blur(40px);

    animation: drift 20s infinite alternate ease-in-out;

}

.nebula-2 {

    position: absolute;

    width: 500px; height: 500px;

    background: radial-gradient(circle, rgba(76, 201, 240, 0.05) 0%, transparent 70%);
  /* [VISUAL LAYER]: Dictating color matrix adjustments and image overlays upon element initialization */

    bottom: 10%; right: 10%;

    filter: blur(50px);

    animation: drift 25s infinite alternate-reverse ease-in-out;

}



@keyframes twinkle {
  /* [ANIMATION LOGIC]: Defining specific timeline mathematical vector transitions over structural changes */

    0% { opacity: 0.5; } 50% { opacity: 1; } 100% { opacity: 0.5; }

}

@keyframes drift {
  /* [ANIMATION LOGIC]: Defining specific timeline mathematical vector transitions over structural changes */

    0% { transform: translate(-50%, -50%) scale(1); } 

    100% { transform: translate(-45%, -55%) scale(1.1); }

}



/* Custom Scrollbar */

::-webkit-scrollbar { width: 8px; height: 8px; }

::-webkit-scrollbar-track { background: #0a0a0a; }
  /* [VISUAL LAYER]: Dictating color matrix adjustments and image overlays upon element initialization */

::-webkit-scrollbar-thumb { background: #333; border-radius: 4px; }
  /* [VISUAL LAYER]: Dictating color matrix adjustments and image overlays upon element initialization */

::-webkit-scrollbar-thumb:hover { background: #555; }
  /* [VISUAL LAYER]: Dictating color matrix adjustments and image overlays upon element initialization */



.custom-scrollbar { scrollbar-width: thin; scrollbar-color: #333 #0a0a0a; }



/* Glassmorphism Refined */

.glass-panel {

    background: rgba(23, 23, 23, 0.6);
  /* [VISUAL LAYER]: Dictating color matrix adjustments and image overlays upon element initialization */

    backdrop-filter: blur(16px);

    border: 1px solid rgba(255, 255, 255, 0.05);

    box-shadow: 0 4px 30px rgba(0, 0, 0, 0.5);

}



/* Animations */

.fade-in-up { animation: fadeInUp 0.5s cubic-bezier(0.16, 1, 0.3, 1); }

@keyframes fadeInUp { from { opacity: 0; transform: translateY(20px); } to { opacity: 1; transform: translateY(0); } }
  /* [ANIMATION LOGIC]: Defining specific timeline mathematical vector transitions over structural changes */



.star-rating i { cursor: pointer; transition: all 0.2s; }

.star-rating i.active { color: #4CC9F0; text-shadow: 0 0 10px #4CC9F0; transform: scale(1.1); }



/* Loader */

.loader-ring {

    width: 40px; height: 40px;

    border: 3px solid rgba(255,255,255,0.1);

    border-top-color: #3b82f6;

    border-radius: 50%;

    animation: spin 1s ease-in-out infinite;

}

@keyframes spin { 100% { transform: rotate(360deg); } }
  /* [ANIMATION LOGIC]: Defining specific timeline mathematical vector transitions over structural changes */



/* Tabs Logic */

.tab-content { display: none !important; height: 100%; flex-direction: column; }

.tab-content.active { display: flex !important; animation: fadeIn 0.3s ease-in; }



/* Selected Tab Style */

.tab-button.active-tab {

    color: #4CC9F0;

    border-bottom-color: #4CC9F0;

    text-shadow: 0 0 10px rgba(76, 201, 240, 0.5);

}





// FILE NAME: server.js
// COMPONENT OVERVIEW:
// Node.js backend server built with Express. Acts as a bridge between the front-end
// and actual MySQL database, authenticating endpoints via cryptographic simulation.
//
// FEATURES AND WORKING:
// 1. Dependency Integration: Loads core body-parser elements to interpret JSON requests.
// 2. Access Controls: Overrides CORS limiting domain re-routing security bugs.
// 3. MySQL DB Instance: Synchronizes queries mapping internal relational row configurations.
// 4. API Endpoints: Handles standard full-scale REST architectural implementations to fetch reviews.

const express = require("express");
  // [COMPONENT WORKING]: Allocated strict memory block for immutable local variable.

const mysql = require("mysql2");
  // [COMPONENT WORKING]: Allocated strict memory block for immutable local variable.

const cors = require("cors");
  // [COMPONENT WORKING]: Allocated strict memory block for immutable local variable.

const bodyParser = require("body-parser");
  // [COMPONENT WORKING]: Allocated strict memory block for immutable local variable.



const app = express();
  // [COMPONENT WORKING]: Allocated strict memory block for immutable local variable.

app.use(cors());

app.use(bodyParser.json({ limit: "50mb" })); // Increased size limit for images



// 1. DATABASE CONNECTION

const db = mysql.createConnection({
  // [COMPONENT WORKING]: Allocated strict memory block for immutable local variable.

  host: "localhost",

  user: "root",

  password: "Mohit@1234", // <--- REPLACE WITH YOUR REAL PASSWORD

  database: "movie_review_system",

});



db.connect((err) => {

  if (err) console.error("❌ DB Error:", err);

  else console.log("✅ MySQL Connected");

});



// --- ADMIN SYSTEM ---

const ADMIN_PASSWORD = "Admin@1234";
  // [COMPONENT WORKING]: Allocated strict memory block for immutable local variable.



app.post("/api/admin/login", (req, res) => {

  if (req.body.password === ADMIN_PASSWORD) {

    res.json({ success: true, token: "admin-token-secret" });

  } else {

    res.status(401).json({ success: false, message: "Invalid Password" });

  }

});



// Advanced Stats with Date Filtering

app.get("/api/admin/stats", (req, res) => {

  const { filter, date } = req.query;

  let timeCondition = "";
  // [COMPONENT WORKING]: Established dynamically updating scope configuration state.



  // MySQL Date Logic

  if (filter === "today") timeCondition = "WHERE DATE(created_at) = CURDATE()";

  else if (filter === "weekly")

    timeCondition = "WHERE created_at >= DATE_SUB(NOW(), INTERVAL 1 WEEK)";

  else if (filter === "monthly")

    timeCondition = "WHERE created_at >= DATE_SUB(NOW(), INTERVAL 1 MONTH)";

  else if (filter === "yearly")

    timeCondition = "WHERE created_at >= DATE_SUB(NOW(), INTERVAL 1 YEAR)";

  else if (filter === "date" && date)

    timeCondition = `WHERE DATE(created_at) = '${date}'`;



  const sqlReviews = `SELECT COUNT(*) as count FROM reviews ${timeCondition}`;
  // [COMPONENT WORKING]: Allocated strict memory block for immutable local variable.

  const sqlBlogs = `SELECT COUNT(*) as count FROM blogs ${timeCondition}`;
  // [COMPONENT WORKING]: Allocated strict memory block for immutable local variable.



  db.query(sqlReviews, (err, rRes) => {

    if (err) return res.status(500).json(err);

    db.query(sqlBlogs, (err, bRes) => {

      if (err) return res.status(500).json(err);

      res.json({ reviews: rRes[0].count, blogs: bRes[0].count });

    });

  });

});



// Fetch Content (Reviews + Blogs)

app.get("/api/admin/content", (req, res) => {

  const q1 =
  // [COMPONENT WORKING]: Allocated strict memory block for immutable local variable.

    "SELECT id, user_name, 'Review' as type, comment as content, created_at FROM reviews ORDER BY created_at DESC";

  const q2 =
  // [COMPONENT WORKING]: Allocated strict memory block for immutable local variable.

    "SELECT id, user_name, 'Blog' as type, title as content, created_at FROM blogs ORDER BY created_at DESC";



  db.query(q1, (err, reviews) => {

    if (err) return res.status(500).json(err);

    db.query(q2, (err, blogs) => {

      if (err) return res.status(500).json(err);

      res.json([...reviews, ...blogs]); // Combine arrays

    });

  });

});



// Delete Content

app.delete("/api/admin/delete/:type/:id", (req, res) => {

  const { type, id } = req.params;

  const table = type === "Review" ? "reviews" : "blogs";
  // [COMPONENT WORKING]: Allocated strict memory block for immutable local variable.

  db.query(`DELETE FROM ${table} WHERE id = ?`, [id], (err, result) => {

    if (err) return res.status(500).json(err);

    res.json({ success: true });

  });

});



// --- PUBLIC API ---



// Reviews

app.get("/reviews/:imdbId", (req, res) => {

  db.query(

    "SELECT * FROM reviews WHERE movie_imdb_id = ? ORDER BY created_at DESC",

    [req.params.imdbId],

    (err, results) => res.json(results),

  );

});



app.post("/add-review", (req, res) => {

  const { movie_imdb_id, user_name, rating, comment } = req.body;

  db.query(

    "INSERT INTO reviews (movie_imdb_id, user_name, rating, comment) VALUES (?, ?, ?, ?)",

    [movie_imdb_id, user_name, rating, comment],

    (err) => {

      if (err) return res.status(500).json(err);

      res.json({ success: true });

    },

  );

});



// Blogs

app.get("/api/blogs", (req, res) => {

  db.query("SELECT * FROM blogs ORDER BY created_at DESC", (err, results) =>

    res.json(results),

  );

});



app.post("/api/blogs", (req, res) => {

  const { user_name, title, content, media } = req.body;

  db.query(

    "INSERT INTO blogs (user_name, title, content, media_url) VALUES (?, ?, ?, ?)",

    [user_name, title, content, media],

    (err) => {

      if (err) return res.status(500).json(err);

      res.json({ success: true });

    },

  );

});



// OTP Simulation

const otpStore = {};
  // [COMPONENT WORKING]: Allocated strict memory block for immutable local variable.

app.post("/api/send-otp", (req, res) => {

  const otp = Math.floor(1000 + Math.random() * 9000);
  // [COMPONENT WORKING]: Allocated strict memory block for immutable local variable.

  otpStore[req.body.phone] = otp;

  console.log(`\n📲 OTP for ${req.body.phone}: ${otp}\n`);

  res.json({ success: true });

});



app.post("/api/verify-otp", (req, res) => {

  if (otpStore[req.body.phone] == req.body.code) {

    delete otpStore[req.body.phone];

    res.json({ success: true });

  } else res.status(400).json({ success: false });

});



app.listen(3000, () => console.log("🚀 Server running on port 3000"));







// FILE NAME: js/config.js
// COMPONENT OVERVIEW:
// A centralized configuration state.
// FEATURES: Contains immutable keys for external API endpoints and storage strings globally.

// Configuration Constants

const OMDB_API_KEY = "2b9ab813"; // Replace if needed
  // [COMPONENT WORKING]: Allocated strict memory block for immutable local variable.



// LocalStorage Keys

const DB_REVIEWS_KEY = 'cinenova_reviews'; 
  // [COMPONENT WORKING]: Allocated strict memory block for immutable local variable.

const DB_BLOGS_KEY = 'cinenova_blogs';
  // [COMPONENT WORKING]: Allocated strict memory block for immutable local variable.

const DB_USER_KEY = 'cinenova_user';
  // [COMPONENT WORKING]: Allocated strict memory block for immutable local variable.





// FILE NAME: js/utils.js
// COMPONENT OVERVIEW:
// Contains reusable abstract functionality logic. 
// FEATURES: Controls DOM manipulation sequences mapping to the Toast Error dialogue popups and Base64 translations.

// --- Helper: Read File as Base64 ---

function readFile(file) {
  // [FEATURE]: Initialization sequence for main operational function logic block 'readFile'

    return new Promise((resolve, reject) => {

        const reader = new FileReader();
  // [COMPONENT WORKING]: Allocated strict memory block for immutable local variable.

        reader.onload = () => resolve(reader.result);

        reader.onerror = reject;

        reader.readAsDataURL(file);

    });

}



// --- Toast Notification ---

function showToast(msg, type = "success") {
  // [FEATURE]: Initialization sequence for main operational function logic block 'showToast'

    const toast = document.getElementById('toast');
  // [COMPONENT WORKING]: Allocated strict memory block for immutable local variable.

    const toastPanel = document.getElementById('toastPanel');
  // [COMPONENT WORKING]: Allocated strict memory block for immutable local variable.

    const toastIcon = document.getElementById('toastIcon');
  // [COMPONENT WORKING]: Allocated strict memory block for immutable local variable.

    const toastTitle = document.getElementById('toastTitle');
  // [COMPONENT WORKING]: Allocated strict memory block for immutable local variable.

    

    document.getElementById('toastMsg').innerText = msg;
  // [DOM MANIPULATION]: Traversing HTML layout extracting element bindings for runtime update.

    

    if (type === "error") {

        toastTitle.innerText = "ERROR";

        toastIcon.className = "fa-solid fa-circle-exclamation text-red-500";

        toastPanel.className = "glass-panel px-6 py-4 rounded-xl border-l-4 border-red-500 flex items-center gap-4 bg-black/90";

    } else {

        toastTitle.innerText = "SUCCESS";

        toastIcon.className = "fa-solid fa-check text-cyber-cyan";

        toastPanel.className = "glass-panel px-6 py-4 rounded-xl border-l-4 border-cyber-cyan flex items-center gap-4 bg-black/90";

    }



    toast.classList.remove('translate-x-full');

    setTimeout(() => toast.classList.add('translate-x-full'), 3000);

}





// FILE NAME: js/state.js
// COMPONENT OVERVIEW:
// Memory management file preserving the current instance's properties.
// FEATURES: Monitors user data and syncs internal document objects rendering uniform avatars across active windows.

// Global State

let currentUser = {
  // [COMPONENT WORKING]: Established dynamically updating scope configuration state.

    name: "Cmdr. Shepard",

    avatar: "https://ui-avatars.com/api/?name=Cmdr+Shepard&background=0a0a12&color=4CC9F0"

};



// Load User Logic

function loadUserProfile() {
  // [FEATURE]: Initialization sequence for main operational function logic block 'loadUserProfile'

    const saved = localStorage.getItem(DB_USER_KEY);
  // [COMPONENT WORKING]: Allocated strict memory block for immutable local variable.

    if(saved) {

        currentUser = JSON.parse(saved);

        updateUserDisplay();

    }

}



// Update UI elements that show user info

function updateUserDisplay() {
  // [FEATURE]: Initialization sequence for main operational function logic block 'updateUserDisplay'

    const avatarEls = ['navbarAvatar', 'reviewUserAvatar', 'profilePreview'];
  // [COMPONENT WORKING]: Allocated strict memory block for immutable local variable.

    avatarEls.forEach(id => {

        const el = document.getElementById(id);
  // [COMPONENT WORKING]: Allocated strict memory block for immutable local variable.

        if(el) el.src = currentUser.avatar;

    });



    const nameDisplay = document.getElementById('reviewUsernameDisplay');
  // [COMPONENT WORKING]: Allocated strict memory block for immutable local variable.

    if(nameDisplay) nameDisplay.innerText = currentUser.name;



    const nameInput = document.getElementById('newUsername');
  // [COMPONENT WORKING]: Allocated strict memory block for immutable local variable.

    if(nameInput) nameInput.value = currentUser.name;

}





// FILE NAME: js/reviews.js
// COMPONENT OVERVIEW:
// The most extensive controller governing movie search endpoints and parsing review arrays arrays.
//
// FEATURES AND WORKING:
// 1. Fetch Integration: Bridges query fields dynamically targeting standard API outputs.
// 2. Rating Mathematical Operations: Assesses HTML star nodes actively replacing classes.
// 3. Caching and Sorting: Translates raw date strings dynamically restructuring objects from high-to-low logic loops.

let currentSearchQuery = '';
  // [COMPONENT WORKING]: Established dynamically updating scope configuration state.

let currentPage = 1;
  // [COMPONENT WORKING]: Established dynamically updating scope configuration state.

let activeMovie = null; 
  // [COMPONENT WORKING]: Established dynamically updating scope configuration state.

let selectedRating = 0;
  // [COMPONENT WORKING]: Established dynamically updating scope configuration state.



// --- Search Core ---

function handleEnter(e) { if(e.key === 'Enter') initSearch(); }
  // [FEATURE]: Initialization sequence for main operational function logic block 'handleEnter'



function initSearch() {
  // [FEATURE]: Initialization sequence for main operational function logic block 'initSearch'

    currentSearchQuery = document.getElementById('searchInput').value.trim();
  // [DOM MANIPULATION]: Traversing HTML layout extracting element bindings for runtime update.

    currentPage = 1;

    fetchMovies();

}



function changePage(delta) {
  // [FEATURE]: Initialization sequence for main operational function logic block 'changePage'

    currentPage += delta;

    if (currentPage < 1) currentPage = 1;

    fetchMovies();

}



async function fetchMovies() {
  // [FEATURE]: Initialization sequence for main operational function logic block 'fetchMovies'

    if (!currentSearchQuery) return;

    const container = document.getElementById('resultsContainer');
  // [COMPONENT WORKING]: Allocated strict memory block for immutable local variable.

    const pagination = document.getElementById('paginationControls');
  // [COMPONENT WORKING]: Allocated strict memory block for immutable local variable.

    

    container.innerHTML = `<div class="col-span-full flex justify-center py-20"><div class="loader-ring"></div></div>`;

    

    try {
  // [SECURITY SANDBOX]: Engaging error-tolerant operational sequence catching runtime faults.

        const res = await fetch(`https://www.omdbapi.com/?s=${encodeURIComponent(currentSearchQuery)}&page=${currentPage}&apikey=${OMDB_API_KEY}`);
  // [COMPONENT WORKING]: Allocated strict memory block for immutable local variable.

        const data = await res.json();
  // [COMPONENT WORKING]: Allocated strict memory block for immutable local variable.



        if (data.Response === 'True') {

            renderGrid(data.Search);

            if (pagination) {

                pagination.classList.remove('hidden');

                document.getElementById('pageIndicator').innerText = currentPage.toString().padStart(2, '0');
  // [DOM MANIPULATION]: Traversing HTML layout extracting element bindings for runtime update.

                document.getElementById('prevBtn').disabled = currentPage === 1;
  // [DOM MANIPULATION]: Traversing HTML layout extracting element bindings for runtime update.

            }

        } else {

            container.innerHTML = `<div class="col-span-full text-center text-gray-400 mt-10 p-6">${data.Error.toUpperCase()}</div>`;

            if (pagination) pagination.classList.add('hidden');

        }

    } catch (e) {
  // [FAILSAFE RECOVERY]: Trapping broken execution flows managing standard graceful fallbacks.

        console.error("Search error:", e);

        container.innerHTML = `<div class="col-span-full text-center text-red-500">CONNECTION LOST</div>`;

    }

}



function renderGrid(movies) {
  // [FEATURE]: Initialization sequence for main operational function logic block 'renderGrid'

    const container = document.getElementById('resultsContainer');
  // [COMPONENT WORKING]: Allocated strict memory block for immutable local variable.

    container.innerHTML = '';

    

    movies.forEach((movie, index) => {

        const poster = (movie.Poster && movie.Poster !== 'N/A') ? movie.Poster : 'https://via.placeholder.com/300x450/111/444?text=NOT+FOUND';
  // [COMPONENT WORKING]: Allocated strict memory block for immutable local variable.

        const el = document.createElement('div');
  // [COMPONENT WORKING]: Allocated strict memory block for immutable local variable.

        el.style.animationDelay = `${index * 50}ms`;

        el.className = 'glass-panel rounded-2xl overflow-hidden group cursor-pointer relative fade-in-up hover:scale-[1.03] hover:shadow-2xl hover:border-white/20 transition-all duration-300';

        el.onclick = () => openMovieDetail(movie);

        el.innerHTML = `

            <div class="h-[360px] overflow-hidden relative">

                <img src="${poster}" class="w-full h-full object-cover group-hover:scale-110 transition-transform duration-700 ease-out">

                <div class="absolute inset-0 bg-gradient-to-t from-black/90 via-black/20 to-transparent opacity-80 group-hover:opacity-100 transition-opacity"></div>

                <div class="absolute inset-0 flex items-center justify-center opacity-0 group-hover:opacity-100 transition-all duration-300 backdrop-blur-sm">

                    <button class="w-14 h-14 rounded-full bg-white/10 border border-white/30 text-white flex items-center justify-center hover:bg-white hover:text-black transition-all shadow-xl backdrop-blur-md"><i class="fa-solid fa-play ml-1"></i></button>

                </div>

                <div class="absolute bottom-4 left-4 right-4 z-10">

                    <h4 class="font-outfit font-bold text-white text-lg leading-tight mb-1 truncate text-shadow" title="${movie.Title}">${movie.Title}</h4>

                    <div class="flex justify-between items-center text-xs font-inter text-gray-300">

                        <span>${movie.Year}</span>

                        <span class="uppercase font-semibold tracking-wider text-white/70 bg-white/10 px-2 py-0.5 rounded-md backdrop-blur-sm shadow-sm">${movie.Type}</span>

                    </div>

                </div>

            </div>

        `;

        container.appendChild(el);

    });

}



// --- Detail View Logic ---

function openMovieDetail(movie) {
  // [FEATURE]: Initialization sequence for main operational function logic block 'openMovieDetail'

    activeMovie = movie;

    const view = document.getElementById('detailView');
  // [COMPONENT WORKING]: Allocated strict memory block for immutable local variable.

    const poster = (movie.Poster && movie.Poster !== 'N/A') ? movie.Poster : 'https://via.placeholder.com/300x450';
  // [COMPONENT WORKING]: Allocated strict memory block for immutable local variable.

    

    document.getElementById('detailPoster').src = poster;
  // [DOM MANIPULATION]: Traversing HTML layout extracting element bindings for runtime update.

    document.getElementById('detailTitle').innerText = movie.Title;
  // [DOM MANIPULATION]: Traversing HTML layout extracting element bindings for runtime update.

    document.getElementById('detailYear').innerText = movie.Year;
  // [DOM MANIPULATION]: Traversing HTML layout extracting element bindings for runtime update.

    document.getElementById('detailType').innerText = movie.Type;
  // [DOM MANIPULATION]: Traversing HTML layout extracting element bindings for runtime update.

    

    view.classList.remove('hidden', 'pointer-events-none');

    setTimeout(() => view.classList.remove('opacity-0', 'scale-95'), 10);

    document.getElementById('mainNavbar').classList.add('opacity-0', '-translate-y-20');
  // [DOM MANIPULATION]: Traversing HTML layout extracting element bindings for runtime update.

    

    document.getElementById('reviewText').value = '';
  // [DOM MANIPULATION]: Traversing HTML layout extracting element bindings for runtime update.

    setRating(0);

    renderReviews();

}



function closeDetailViewForce() {
  // [FEATURE]: Initialization sequence for main operational function logic block 'closeDetailViewForce'

    const view = document.getElementById('detailView');
  // [COMPONENT WORKING]: Allocated strict memory block for immutable local variable.

    view.classList.add('opacity-0', 'scale-95');

    document.getElementById('mainNavbar').classList.remove('opacity-0', '-translate-y-20');
  // [DOM MANIPULATION]: Traversing HTML layout extracting element bindings for runtime update.

    setTimeout(() => {

        view.classList.add('hidden', 'pointer-events-none');

        activeMovie = null;

    }, 300);

}



function closeDetailView(event) {
  // [FEATURE]: Initialization sequence for main operational function logic block 'closeDetailView'

    if (event.target.id === 'detailView') closeDetailViewForce();

}



// --- Reviews Logic ---

function getReviews() {
  // [FEATURE]: Initialization sequence for main operational function logic block 'getReviews'

    const stored = localStorage.getItem(DB_REVIEWS_KEY);
  // [COMPONENT WORKING]: Allocated strict memory block for immutable local variable.

    return stored ? JSON.parse(stored) : [];

}



function initStarRating() {
  // [FEATURE]: Initialization sequence for main operational function logic block 'initStarRating'

    const stars = document.querySelectorAll('#starInput i');
  // [COMPONENT WORKING]: Allocated strict memory block for immutable local variable.

    stars.forEach(star => {

        star.addEventListener('click', () => setRating(parseInt(star.dataset.val)));

        star.addEventListener('mouseover', () => highlightStars(parseInt(star.dataset.val)));

        star.addEventListener('mouseleave', () => highlightStars(selectedRating));

    });

}



function setRating(val) {
  // [FEATURE]: Initialization sequence for main operational function logic block 'setRating'

    selectedRating = val;

    highlightStars(val);

}



function highlightStars(count) {
  // [FEATURE]: Initialization sequence for main operational function logic block 'highlightStars'

    const stars = document.querySelectorAll('#starInput i');
  // [COMPONENT WORKING]: Allocated strict memory block for immutable local variable.

    stars.forEach(star => {

        const val = parseInt(star.dataset.val);
  // [COMPONENT WORKING]: Allocated strict memory block for immutable local variable.

        if (val <= count) {

            star.classList.add('active');

            star.classList.replace('text-gray-600', 'text-cyber-cyan');

        } else {

            star.classList.remove('active');

            star.classList.replace('text-cyber-cyan', 'text-gray-600');

        }

    });

}



function submitReview() {
  // [FEATURE]: Initialization sequence for main operational function logic block 'submitReview'

    if (!isVerified) {

        showToast("Verification Required: Verify OTP in Profile", "error");

        return; 

    }



    const text = document.getElementById('reviewText').value.trim();
  // [COMPONENT WORKING]: Allocated strict memory block for immutable local variable.

    if (!selectedRating) return showToast("Error: Rating Required", "error");

    if (!text) return showToast("Error: Transmission Empty", "error");



    const reviews = getReviews();
  // [COMPONENT WORKING]: Allocated strict memory block for immutable local variable.

    reviews.push({

        id: Date.now(),

        movie_id: activeMovie.imdbID,

        user: currentUser.name,

        avatar: currentUser.avatar,

        rating: selectedRating,

        content: text,

        likes: 0,

        dislikes: 0,

        date: new Date().toISOString()

    });

    localStorage.setItem(DB_REVIEWS_KEY, JSON.stringify(reviews));
  // [DATA PERSISTENCE]: Executing cache writing saving values onto localized browser drives.

    

    document.getElementById('reviewText').value = '';
  // [DOM MANIPULATION]: Traversing HTML layout extracting element bindings for runtime update.

    setRating(0);

    renderReviews();

    showToast("Review Transmitted");

}



function renderReviews() {
  // [FEATURE]: Initialization sequence for main operational function logic block 'renderReviews'

    if (!activeMovie) return;

    const sortMode = document.getElementById('sortSelect').value;
  // [COMPONENT WORKING]: Allocated strict memory block for immutable local variable.

    let reviews = getReviews().filter(r => r.movie_id === activeMovie.imdbID);
  // [COMPONENT WORKING]: Established dynamically updating scope configuration state.



    if (sortMode === 'newest') reviews.sort((a,b) => new Date(b.date) - new Date(a.date));

    if (sortMode === 'oldest') reviews.sort((a,b) => new Date(a.date) - new Date(b.date));

    if (sortMode === 'highest') reviews.sort((a,b) => b.rating - a.rating);

    if (sortMode === 'lowest') reviews.sort((a,b) => a.rating - b.rating);



    const container = document.getElementById('reviewsList');
  // [COMPONENT WORKING]: Allocated strict memory block for immutable local variable.

    container.innerHTML = '';



    if (reviews.length === 0) {

        container.innerHTML = `<div class="text-center py-10 opacity-50"><p class="font-inter text-sm">NO DATA FOUND</p></div>`;

        return;

    }



    reviews.forEach(r => {

        const stars = Array(5).fill(0).map((_, i) => `<i class="fa-solid fa-star ${i < r.rating ? 'text-cyber-cyan' : 'text-gray-800'} text-[10px]"></i>`).join('');
  // [COMPONENT WORKING]: Allocated strict memory block for immutable local variable.

        const date = new Date(r.date).toLocaleDateString();
  // [COMPONENT WORKING]: Allocated strict memory block for immutable local variable.

        const el = document.createElement('div');
  // [COMPONENT WORKING]: Allocated strict memory block for immutable local variable.

        el.className = 'bg-white/5 border border-white/5 rounded-xl p-4 fade-in-up';

        el.innerHTML = `

            <div class="flex justify-between items-start mb-2">

                <div class="flex items-center gap-3">

                    <img src="${r.avatar || 'https://ui-avatars.com/api/?background=000&color=fff'}" class="w-8 h-8 rounded-full border border-white/10 object-cover">

                    <div><div class="font-bold text-sm text-gray-200 font-outfit">${r.user}</div><div class="flex gap-1 mt-0.5">${stars}</div></div>

                </div>

                <span class="text-[10px] font-mono text-gray-600">${date}</span>

            </div>

            <p class="text-gray-300 text-sm font-inter pl-11">${r.content}</p>

        `;

        container.appendChild(el);

    });

}





// FILE NAME: js/blog.js
// COMPONENT OVERVIEW:
// System operations mapped toward the Blog Transmission functionality handling rich-media outputs.
// FEATURES: Implements image blob abstractions pushing dynamic binary strings into HTML sources.

// FETCH BLOGS

async function renderBlogFeed() {
  // [FEATURE]: Initialization sequence for main operational function logic block 'renderBlogFeed'

  const container = document.getElementById("blogFeed");
  // [COMPONENT WORKING]: Allocated strict memory block for immutable local variable.

  container.innerHTML = '<div class="loader-ring mx-auto my-10"></div>';



  try {
  // [SECURITY SANDBOX]: Engaging error-tolerant operational sequence catching runtime faults.

    const res = await fetch("http://localhost:3000/api/blogs");
  // [COMPONENT WORKING]: Allocated strict memory block for immutable local variable.

    const blogs = await res.json();
  // [COMPONENT WORKING]: Allocated strict memory block for immutable local variable.



    container.innerHTML = "";

    if (!blogs || blogs.length === 0) {

      container.innerHTML = `<div class="text-center text-gray-500 font-tech">NO SIGNAL DETECTED</div>`;

      return;

    }



    blogs.forEach((post) => {

      let mediaHtml = "";
  // [COMPONENT WORKING]: Established dynamically updating scope configuration state.

      if (post.media_url) {

        if (post.media_url.startsWith("data:video")) {

          mediaHtml = `<video src="${post.media_url}" controls class="w-full rounded-lg mt-3 border border-white/10"></video>`;

        } else {

          mediaHtml = `<img src="${post.media_url}" class="w-full rounded-lg mt-3 border border-white/10 object-cover max-h-96">`;

        }

      }

      const date = new Date(post.created_at).toLocaleDateString();
  // [COMPONENT WORKING]: Allocated strict memory block for immutable local variable.



      const el = document.createElement("div");
  // [COMPONENT WORKING]: Allocated strict memory block for immutable local variable.

      el.className = "glass-panel p-6 rounded-xl border border-white/5 fade-in-up";

      el.innerHTML = `

                <div class="flex items-center gap-3 mb-4">

                     <div class="w-10 h-10 rounded-full bg-cyber-purple flex items-center justify-center font-bold text-white border border-white/10">${post.user_name.charAt(0)}</div>

                    <div><div class="font-outfit text-sm text-cyber-cyan">${post.user_name}</div><div class="font-mono text-[10px] text-gray-500">${date}</div></div>

                </div>

                <h3 class="font-outfit text-lg text-white mb-2">${post.title}</h3>

                <p class="font-inter text-gray-300 whitespace-pre-wrap">${post.content}</p>

                ${mediaHtml}

            `;

      container.appendChild(el);

    });

  } catch (e) {
  // [FAILSAFE RECOVERY]: Trapping broken execution flows managing standard graceful fallbacks.

    console.error("Blog fetch error:", e);

    container.innerHTML = `<div class="text-red-500 text-center">OFFLINE</div>`;

  }

}



// UPLOAD BLOG

async function submitBlogPost() {
  // [FEATURE]: Initialization sequence for main operational function logic block 'submitBlogPost'

  if (!isVerified) {

    showToast("Verification Required: Verify OTP in Profile", "error");

    switchMainTab('profile');

    return;

  }



  const title = document.getElementById("blogTitleInput").value.trim();
  // [COMPONENT WORKING]: Allocated strict memory block for immutable local variable.

  const content = document.getElementById("blogContentInput").value.trim();
  // [COMPONENT WORKING]: Allocated strict memory block for immutable local variable.

  const mediaInput = document.getElementById("blogMediaInput");
  // [COMPONENT WORKING]: Allocated strict memory block for immutable local variable.



  if (!title || !content) return showToast("Title & Content Required");



  let mediaData = null;
  // [COMPONENT WORKING]: Established dynamically updating scope configuration state.

  if (mediaInput.files && mediaInput.files[0]) {

    try {
  // [SECURITY SANDBOX]: Engaging error-tolerant operational sequence catching runtime faults.

      mediaData = await readFile(mediaInput.files[0]);

    } catch (e) {
  // [FAILSAFE RECOVERY]: Trapping broken execution flows managing standard graceful fallbacks.

      return showToast("Error reading file");

    }

  }



  const payload = {
  // [COMPONENT WORKING]: Allocated strict memory block for immutable local variable.

    user_name: currentUser.name,

    title: title,

    content: content,

    media: mediaData,

  };



  try {
  // [SECURITY SANDBOX]: Engaging error-tolerant operational sequence catching runtime faults.

    const res = await fetch("http://localhost:3000/api/blogs", {
  // [COMPONENT WORKING]: Allocated strict memory block for immutable local variable.

      method: "POST",

      headers: { "Content-Type": "application/json" },

      body: JSON.stringify(payload),

    });

    if (res.ok) {

      document.getElementById("blogTitleInput").value = "";
  // [DOM MANIPULATION]: Traversing HTML layout extracting element bindings for runtime update.

      document.getElementById("blogContentInput").value = "";
  // [DOM MANIPULATION]: Traversing HTML layout extracting element bindings for runtime update.

      mediaInput.value = "";

      renderBlogFeed();

      showToast("Transmission Uploaded");

    }

  } catch (e) {
  // [FAILSAFE RECOVERY]: Trapping broken execution flows managing standard graceful fallbacks.

    showToast("Upload Failed");

  }

}



document.addEventListener("DOMContentLoaded", renderBlogFeed);







// FILE NAME: js/profile.js
// COMPONENT OVERVIEW:
// Authentication layer mapping specialized Multi-Factor routines dynamically controlling user verifications.
// FEATURES: Intercepts POST sequences triggering localhost backend verification hooks tracking temporary memory codes.

let isVerified = false;
  // [COMPONENT WORKING]: Established dynamically updating scope configuration state.



// 1. Send OTP

async function sendOTP() {
  // [FEATURE]: Initialization sequence for main operational function logic block 'sendOTP'

  const phone = document.getElementById("phoneNumber").value;
  // [COMPONENT WORKING]: Allocated strict memory block for immutable local variable.

  if (phone.length < 10) return showToast("Invalid Phone Number");



  try {
  // [SECURITY SANDBOX]: Engaging error-tolerant operational sequence catching runtime faults.

    const res = await fetch("http://localhost:3000/api/send-otp", {
  // [COMPONENT WORKING]: Allocated strict memory block for immutable local variable.

      method: "POST",

      headers: { "Content-Type": "application/json" },

      body: JSON.stringify({ phone }),

    });

    const data = await res.json();
  // [COMPONENT WORKING]: Allocated strict memory block for immutable local variable.



    if (data.success) {

      document.getElementById("phoneStep").classList.add("hidden");
  // [DOM MANIPULATION]: Traversing HTML layout extracting element bindings for runtime update.

      document.getElementById("otpVerifyStep").classList.remove("hidden");
  // [DOM MANIPULATION]: Traversing HTML layout extracting element bindings for runtime update.

      showToast("OTP Sent (Check VS Code Terminal)");

    }

  } catch (e) {
  // [FAILSAFE RECOVERY]: Trapping broken execution flows managing standard graceful fallbacks.

    showToast("Server Error: Ensure Backend is Running");

  }

}



// 2. Verify OTP

async function verifyOTP() {
  // [FEATURE]: Initialization sequence for main operational function logic block 'verifyOTP'

  const phone = document.getElementById("phoneNumber").value;
  // [COMPONENT WORKING]: Allocated strict memory block for immutable local variable.

  const code = document.getElementById("otpCode").value;
  // [COMPONENT WORKING]: Allocated strict memory block for immutable local variable.



  try {
  // [SECURITY SANDBOX]: Engaging error-tolerant operational sequence catching runtime faults.

    const res = await fetch("http://localhost:3000/api/verify-otp", {
  // [COMPONENT WORKING]: Allocated strict memory block for immutable local variable.

      method: "POST",

      headers: { "Content-Type": "application/json" },

      body: JSON.stringify({ phone, code }),

    });

    const data = await res.json();
  // [COMPONENT WORKING]: Allocated strict memory block for immutable local variable.



    if (data.success) {

      isVerified = true;

      document.getElementById("otpSection").classList.add("hidden");
  // [DOM MANIPULATION]: Traversing HTML layout extracting element bindings for runtime update.

      document.getElementById("profileFormSection").classList.remove("hidden");
  // [DOM MANIPULATION]: Traversing HTML layout extracting element bindings for runtime update.

      showToast("Identity Verified");

      loadUserProfile(); // Load data

    } else {

      showToast("Invalid Code");

    }

  } catch (e) {
  // [FAILSAFE RECOVERY]: Trapping broken execution flows managing standard graceful fallbacks.

    showToast("Verification Failed");

  }

}



function resetOTP() {
  // [FEATURE]: Initialization sequence for main operational function logic block 'resetOTP'

  document.getElementById("phoneStep").classList.remove("hidden");
  // [DOM MANIPULATION]: Traversing HTML layout extracting element bindings for runtime update.

  document.getElementById("otpVerifyStep").classList.add("hidden");
  // [DOM MANIPULATION]: Traversing HTML layout extracting element bindings for runtime update.

}



// 3. Save Profile

async function saveUserProfile() {
  // [FEATURE]: Initialization sequence for main operational function logic block 'saveUserProfile'

  if (!isVerified) return showToast("Verification Required");



  const name = document.getElementById("newUsername").value.trim();
  // [COMPONENT WORKING]: Allocated strict memory block for immutable local variable.

  const avatarInput = document.getElementById("newAvatar");
  // [COMPONENT WORKING]: Allocated strict memory block for immutable local variable.



  if (!name) return showToast("Error: Designation Required");

  currentUser.name = name;



  if (avatarInput.files && avatarInput.files[0]) {

    const file = avatarInput.files[0];
  // [COMPONENT WORKING]: Allocated strict memory block for immutable local variable.

    if (file.size > 2097152) return showToast("Error: Image too large (>2MB)");

    try {
  // [SECURITY SANDBOX]: Engaging error-tolerant operational sequence catching runtime faults.

      currentUser.avatar = await readFile(file);

    } catch (e) {
  // [FAILSAFE RECOVERY]: Trapping broken execution flows managing standard graceful fallbacks.

      return showToast("Error reading file");

    }

  } else if (!currentUser.avatar) {

    currentUser.avatar = `https://ui-avatars.com/api/?name=${encodeURIComponent(

      name

    )}&background=0a0a12&color=4CC9F0`;

  }



  localStorage.setItem(DB_USER_KEY, JSON.stringify(currentUser));
  // [DATA PERSISTENCE]: Executing cache writing saving values onto localized browser drives.

  updateUserDisplay();



  const msg = document.getElementById("profileSavedMsg");
  // [COMPONENT WORKING]: Allocated strict memory block for immutable local variable.

  msg.classList.remove("hidden");

  setTimeout(() => msg.classList.add("hidden"), 3000);



  showToast("Identity Configured");

  avatarInput.value = "";

}







// FILE NAME: js/admin.js
// COMPONENT OVERVIEW:
// High level system overrides executing database manipulations requiring elevated permissions.
// FEATURES: Generates restricted HTML structural maps exposing 'Delete' functionality tied directly to SQL removal requests.

let adminToken = null;
  // [COMPONENT WORKING]: Established dynamically updating scope configuration state.



// 1. LOGIN

async function adminLogin() {
  // [FEATURE]: Initialization sequence for main operational function logic block 'adminLogin'

  const password = document.getElementById("adminPasswordInput").value;
  // [COMPONENT WORKING]: Allocated strict memory block for immutable local variable.

  try {
  // [SECURITY SANDBOX]: Engaging error-tolerant operational sequence catching runtime faults.

    const res = await fetch("http://localhost:3000/api/admin/login", {
  // [COMPONENT WORKING]: Allocated strict memory block for immutable local variable.

      method: "POST",

      headers: { "Content-Type": "application/json" },

      body: JSON.stringify({ password }),

    });

    const data = await res.json();
  // [COMPONENT WORKING]: Allocated strict memory block for immutable local variable.



    if (data.success) {

      adminToken = data.token;

      document.getElementById("adminLoginOverlay").classList.add("hidden");
  // [DOM MANIPULATION]: Traversing HTML layout extracting element bindings for runtime update.

      document.getElementById("adminPanelContent").classList.remove("hidden");
  // [DOM MANIPULATION]: Traversing HTML layout extracting element bindings for runtime update.

      refreshAdminData();

    } else {

      showToast("Access Denied: Wrong Password", "error");

    }

  } catch (e) {
  // [FAILSAFE RECOVERY]: Trapping broken execution flows managing standard graceful fallbacks.

    showToast("Server Error", "error");

  }

}



// 2. FETCH DATA & FILTERS

async function refreshAdminData() {
  // [FEATURE]: Initialization sequence for main operational function logic block 'refreshAdminData'

  if (!adminToken) return;



  const filter = document.getElementById("statsFilter").value;
  // [COMPONENT WORKING]: Allocated strict memory block for immutable local variable.

  let query = `?filter=${filter}`;
  // [COMPONENT WORKING]: Established dynamically updating scope configuration state.



  if (filter === "date") {

    const dateVal = document.getElementById("statsDateInput").value;
  // [COMPONENT WORKING]: Allocated strict memory block for immutable local variable.

    if (!dateVal) return;

    query += `&date=${dateVal}`;

  }



  try {
  // [SECURITY SANDBOX]: Engaging error-tolerant operational sequence catching runtime faults.

    // Stats

    const statsRes = await fetch(
  // [COMPONENT WORKING]: Allocated strict memory block for immutable local variable.

      `http://localhost:3000/api/admin/stats${query}`,

    );

    const stats = await statsRes.json();
  // [COMPONENT WORKING]: Allocated strict memory block for immutable local variable.

    document.getElementById("adminReviewCount").innerText = stats.reviews;
  // [DOM MANIPULATION]: Traversing HTML layout extracting element bindings for runtime update.

    document.getElementById("adminBlogCount").innerText = stats.blogs;
  // [DOM MANIPULATION]: Traversing HTML layout extracting element bindings for runtime update.



    // Table Data

    const contentRes = await fetch("http://localhost:3000/api/admin/content");
  // [COMPONENT WORKING]: Allocated strict memory block for immutable local variable.

    const content = await contentRes.json();
  // [COMPONENT WORKING]: Allocated strict memory block for immutable local variable.

    renderAdminTable(content);

  } catch (e) {
  // [FAILSAFE RECOVERY]: Trapping broken execution flows managing standard graceful fallbacks.

    console.error(e);

  }

}



function renderAdminTable(data) {
  // [FEATURE]: Initialization sequence for main operational function logic block 'renderAdminTable'

  const tbody = document.getElementById("adminTableBody");
  // [COMPONENT WORKING]: Allocated strict memory block for immutable local variable.

  tbody.innerHTML = "";



  if (data.length === 0) {

    tbody.innerHTML =

      '<tr><td colspan="5" class="text-center p-4 text-gray-500">No records found.</td></tr>';

    return;

  }



  data.forEach((item) => {

    const typeClass =
  // [COMPONENT WORKING]: Allocated strict memory block for immutable local variable.

      item.type === "Review" ? "text-cyber-cyan" : "text-purple-400";

    const tr = document.createElement("tr");
  // [COMPONENT WORKING]: Allocated strict memory block for immutable local variable.

    tr.className = "border-b border-white/5 hover:bg-white/5 transition-colors";

    tr.innerHTML = `

            <td class="p-3 text-gray-500">#${item.id}</td>

            <td class="p-3 font-bold ${typeClass}">${item.type}</td>

            <td class="p-3">${item.user_name}</td>

            <td class="p-3 truncate max-w-[200px] text-xs text-gray-400">${item.content}</td>

            <td class="p-3">

                <button onclick="deleteContent('${item.type}', '${item.id}')" class="text-red-500 hover:text-red-400 text-xs border border-red-500/50 px-2 py-1 rounded hover:bg-red-500/10 mb-1 inline-block">DELETE</button>

            </td>

        `;

    tbody.appendChild(tr);

  });

}



// 3. DELETE

async function deleteContent(type, id) {
  // [FEATURE]: Initialization sequence for main operational function logic block 'deleteContent'

  // Use a softer confirmation to avoid browser popup blockers

  if (!window.confirm(`Permanently delete this ${type}?`)) return;

  

  try {
  // [SECURITY SANDBOX]: Engaging error-tolerant operational sequence catching runtime faults.

    const res = await fetch(`http://localhost:3000/api/admin/delete/${type}/${id}`, {
  // [COMPONENT WORKING]: Allocated strict memory block for immutable local variable.

      method: "DELETE",

      headers: {

        "Content-Type": "application/json",

        "Authorization": `Bearer ${adminToken}`

      }

    });

    

    if (!res.ok) throw new Error("Delete request failed");

    showToast(`${type} Deleted`);

    

    if (type === "Review") {

        let reviews = JSON.parse(localStorage.getItem(DB_REVIEWS_KEY)) || [];
  // [COMPONENT WORKING]: Established dynamically updating scope configuration state.

        reviews = reviews.filter(r => String(r.id) !== String(id));

        localStorage.setItem(DB_REVIEWS_KEY, JSON.stringify(reviews));
  // [DATA PERSISTENCE]: Executing cache writing saving values onto localized browser drives.

        if (typeof renderReviews === "function") renderReviews();

    }

    

    refreshAdminData();

    if (type === "Blog" && typeof renderBlogFeed === "function") {

        renderBlogFeed(); // Keep the public feed in sync with backend deletions

    }

  } catch (e) {
  // [FAILSAFE RECOVERY]: Trapping broken execution flows managing standard graceful fallbacks.

    console.error("Delete Error:", e);

    showToast("Delete Failed", "error");

  }

}



function handleFilterChange() {
  // [FEATURE]: Initialization sequence for main operational function logic block 'handleFilterChange'

  const filter = document.getElementById("statsFilter").value;
  // [COMPONENT WORKING]: Allocated strict memory block for immutable local variable.

  const dateInput = document.getElementById("statsDateInput");
  // [COMPONENT WORKING]: Allocated strict memory block for immutable local variable.

  if (filter === "date") {

    dateInput.classList.remove("hidden");

  } else {

    dateInput.classList.add("hidden");

    refreshAdminData();

  }

}







// FILE NAME: js/main.js
// COMPONENT OVERVIEW:
// Initial Startup Bootstrapper execution block.
// FEATURES: Registers base configurations initializing demo elements directly triggered via DOMContentLoaded parameters.

// --- Initialization ---

document.addEventListener('DOMContentLoaded', () => {

    initStarRating();

    resetApp(); // Load default trending movies on startup early

    initDemoBlogs(); 

    loadUserProfile();

    updateUserDisplay();

    try {
  // [SECURITY SANDBOX]: Engaging error-tolerant operational sequence catching runtime faults.

        renderBlogFeed();

    } catch(e) { console.error("Blog feed init error:", e); }
  // [FAILSAFE RECOVERY]: Trapping broken execution flows managing standard graceful fallbacks.

});



// --- Tab Navigation ---

function switchMainTab(tabId) {
  // [FEATURE]: Initialization sequence for main operational function logic block 'switchMainTab'

    document.querySelectorAll('.tab-content').forEach(el => el.classList.remove('active'));

    document.querySelectorAll('.tab-button').forEach(el => {

        el.classList.remove('active-tab');

        el.classList.remove('text-cyber-cyan', 'border-cyber-cyan');

        el.classList.add('text-gray-400', 'border-transparent');

    });



    document.getElementById(`tab-${tabId}`).classList.add('active');
  // [DOM MANIPULATION]: Traversing HTML layout extracting element bindings for runtime update.

    

    const btn = document.getElementById(`nav-${tabId}`);
  // [COMPONENT WORKING]: Allocated strict memory block for immutable local variable.

    if (btn) {

        btn.classList.add('active-tab', 'text-cyber-cyan', 'border-cyber-cyan');

        btn.classList.remove('text-gray-400', 'border-transparent');

    }

}



function resetApp() {
  // [FEATURE]: Initialization sequence for main operational function logic block 'resetApp'

    document.getElementById('searchInput').value = '';
  // [DOM MANIPULATION]: Traversing HTML layout extracting element bindings for runtime update.

    loadDemoData();

}



function loadDemoData() {
  // [FEATURE]: Initialization sequence for main operational function logic block 'loadDemoData'

    const demos = [
  // [COMPONENT WORKING]: Allocated strict memory block for immutable local variable.

        { Title: "Oppenheimer", Year: "2023", imdbID: "tt15398776", Type: "movie", Poster: "https://m.media-amazon.com/images/M/MV5BMDBmYTZjNjUtN2M1MS00MTQ2LTk2ODgtNzc2M2QyZGE5NTVjXkEyXkFqcGdeQXVyMzQwMTY2Nzk@._V1_SX300.jpg" },

        { Title: "Spider-Man: Across the Spider-Verse", Year: "2023", imdbID: "tt9362722", Type: "movie", Poster: "https://m.media-amazon.com/images/M/MV5BMzI0NmVkMjEtYmY4MS00ZDMxLTlkZmRE1MDM5MTZmZTM4_V1_SX300.jpg" },

        { Title: "Dune: Part Two", Year: "2024", imdbID: "tt15239678", Type: "movie", Poster: "https://m.media-amazon.com/images/M/MV5BODdjMjM3NGQtZDA5OC00NGE4LWIyZDQtZjYwOGZlMTM5ZTQ1XkEyXkFqcGdeQXVyODE5NzE3OTE@._V1_SX300.jpg" },

        { Title: "The Batman", Year: "2022", imdbID: "tt1877830", Type: "movie", Poster: "https://m.media-amazon.com/images/M/MV5BMDdmMTBiNTYtMDIzNi00NGVlLWIzMDYtZTk3MTQ3NGQxZGEwXkEyXkFqcGdeQXVyMzMwOTU5MDk@._V1_SX300.jpg" },

        { Title: "Everything Everywhere All at Once", Year: "2022", imdbID: "tt6710474", Type: "movie", Poster: "https://m.media-amazon.com/images/M/MV5BYTdiOTIyZTQtNmQ1OS00NjZlLWIyMTgtYzk5Y2M3ZDVmOTE3XkEyXkFqcGdeQXVyMTEyMjM2NDc2._V1_SX300.jpg" }

    ];

    renderGrid(demos);

    const pag = document.getElementById('paginationControls');
  // [COMPONENT WORKING]: Allocated strict memory block for immutable local variable.

    if(pag) pag.classList.add('hidden');

}



